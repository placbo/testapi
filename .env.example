# Database Configuration - PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=postgres
DB_PASSWORD=example

# Server Configuration
PORT=8083

# Note: Copy this file to .env and modify the values as needed
# The application will use these environment variables to connect to your database
